1. baseline 20088164  3个epoch 0.06
# encoder换成了deformable 71 epoch 0.229 训练时间从30分钟减少到23分钟，减少了23%
04-02
# 尝试了直接使用0层的特征作为transformer encoder输入发现效果不好
----
# 尝试了基于预测值的topk query获取效果一般
混合策略 (Hybrid Strategies):
结合多种方法。例如，一部分参考点来自 Top-K，另一部分来自固定的稀疏网格以保证覆盖性，还有一部分是可学习的。
优点： 可能取长补短。
缺点： 设计和调优更复杂。
04-05
* 使用loss_quality: 0.263 23分钟
* 使用deformable_encoder:

04-06
* 使用deformable 基本不掉点，使用encoder gqa = 4，掉点4% 47%
* 使用quality head 基本不涨点
* 使用decoder gqa = 4

----
* 使用gate融合m kv_head = 4 涨点5%左右，使用BottleNeck Gate需要将比例调整到0.9左右
* 尝试使用bottleneck gate, decoder也使用kv_head
* FFN改为DW卷积

----
使用MobileNet替换resnet18
FPN融合进入Deformble Encoder
----
动态query个数 不太行，效果不好
----
04.07
FPN融合： 尝试ASPP 动态融合

offset generate network
self.conv_offset = nn.Sequential(
    nn.Conv2d(self.n_group_channels, self.n_group_channels, kk, stride, pad_size, groups=self.n_group_channels),
    LayerNormProxy(self.n_group_channels),
    nn.GELU(),
    nn.Conv2d(self.n_group_channels, 2, 1, 1, 0, bias=False)
)

尝试将其他 第0-(n-1)层特征下采样加到最后一层中进行编码 效果不好，特征可能丢失的比较多
尝试进行动态融合
----

04.08 GQA只用于解码器，同时增加query个数 GQA先不尝试了，计算量并不会减少

网络层中交替应用 Local Attention 和 Deformable Attention 尝试Local Attention 和 Deformable Attention的交替执行；

---
04.09
尝试多层特征交叉注意力融合 添加交叉注意力，效果有提升 到26%，而且后续还有提升的可能
去掉FPN融合 和自注意力，只有 16% 掉了10%
---
04.10
将多分类Topk的改为二分类，增加query数 16.7%指标看起来还可以
使用基于中心点的位置表示 + 多尺度信息能够表示不同大小物体的位置  + 基于注意力机制根据图像内容动态调整位置编码 精度到了20% 不错 缺点：不稳定
---
04.11
FPN融合 尝试了其他的发现都不太稳定，好像还是PAN比较稳定 正常应该可以到24%左右，实际上17.2%
第二篇论文实验准备
第一篇论文开始做消融实验

使用deformable encoder 收敛时间缩短了一半